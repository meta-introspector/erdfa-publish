[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "erdfa-publish"
version = "0.1.0"
description = "SOLFUNMEME erdfa-publish Python bindings (Hecke, IPFS, DASL, orbifold)"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
